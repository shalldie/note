import {table} from 'table';

const data = [
    ['0A', '0B', '中文测试'],
    ['中文', '1B', '1C'],
    ['2A', 'emoji 😂', '2C']
];

console.log(table(data));

console['logg']('sth error');

// eiseg 专门用于图像分割。
