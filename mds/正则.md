## 对于子组，如果后面带了数量，只匹配最后一项

https://www.regular-expressions.info/captureall.html

    12345
    (\d)*
    来匹配，遇到1，\d匹配成功，保存分组，因为是贪婪模式，则继续匹配，直到失配为止。则整个表达式的结果是匹配了12345。每多匹配一次，则\d分组多一个结果，这里有3个内容：
    1.整个表达式匹配内容：12345
    2.分组(\d)
    3.分组的多次捕获（Captue）是5次，分别是：1，2，3，4，5
    显示分组捕获，默认显示最后一次（栈顶捕获）
