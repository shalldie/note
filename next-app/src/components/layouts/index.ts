import {enumxFactory} from '~/libs/enumFactory';
import {LayoutDefault} from './default';

export const ELayoutTemplte = enumxFactory({
    /** 默认模板 */
    default: {id: 'default', com: LayoutDefault}
});
